# Project dependencies for the Selenium automation framework.
# Install these packages with: pip install -r requirements.txt

selenium
pytest
allure-pytest
pytest-rerunfailures
webdriver-manager

# Optional: For type hints and linting (recommended for professional projects)
# mypy -  tool for checking type hints in Python.
# flake8 - tool for enforcing coding style in Python.
